{
  "name": "dc-membership-management",
  "version": "0.1.1",
  "scripts": {
    "css": "node-sass src/scss/main.scss css/member-admin.css --output-style compressed",
    "watch": "npm run css -- --watch",
    "dist": "npm pack && tar -xzf .tgz --strip-components=1 --inlude='./css/*' --include='./js/*'"
  },
  "dependencies": {
    "node-sass": "^8.0.0"
  },
  "devDependencies": {
    "tar": "^6.2.0"
  }
}
